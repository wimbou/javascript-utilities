/**
 ** @author {your name}
 ** @function
 ** @return {boolean}
 * @param args
 **/
export default function isNumber(number) {
  if (typeof number === "number") {
    return true;
  } else {
    return false;
  }
}
